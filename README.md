This Python script is a multi-threaded port scanner that checks which ports are open on a given target host.

Working:
1. The user provides a target IP or domain.
2. The user enters a range of ports (e.g., 20 to 100).
3. The script:
   - Scans each port using multithreading (for speed).
   - Prints open ports.


